{
  "name": "template-javascript",
  "version": "0.0.0",
  "repository": "ninetails/template-javascript",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "husky": "^3.0.0",
    "rimraf": "^2.6.3"
  },
  "scripts": {
    "clean": "rimraf node_modules || :",
    "reset": "$npm_execpath run clean && $npm_execpath install"
  }
}
